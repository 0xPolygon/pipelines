---
'slack-notify': patch
'upsert-changeset-comment': patch
---

Initial import from `0xPolygon/apps-team-workflows`. Both packages compile to the ncc dist bundles consumed by `apps-slack-merge-notify.yml` and `apps-changeset-check.yml` respectively.
